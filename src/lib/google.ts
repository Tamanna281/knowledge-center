// Google token verification removed - Google login disabled
export const verifyGoogleToken = async (token: string) => {
    // Previously used to verify Google ID tokens. Google login has been removed.
    void token
    return null
}
